module.exports = {

	init: function(mediator){
		//var sockJsClient = require("sockJsClient");
		//var config = mediator.config();

		// init the sock js connection based on configuration (host, port)
		// sockjs.listen(config.port, config.ip)
	},
	publish: function(){


	}


}